# Core — everything needed to run the API, the validators and the verdict engine.
# This installs light and fast, and is all the CI job needs.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
alembic>=1.13
python-multipart>=0.0.9
sse-starlette>=2.0

# Cryptography — Aadhaar signature verification and the audit hash chain.
# Never replaced with hand-written primitives.
cryptography>=42.0
pyOpenSSL>=24.0

# Imaging — preprocessing, the quality gate, QR decoding and forensics.
# opencv-python-headless rather than opencv-python: no GUI in a container.
opencv-python-headless>=4.9
pillow>=10.2
numpy>=1.26

# Testing and linting
pytest>=8.0
httpx>=0.27
ruff>=0.4
