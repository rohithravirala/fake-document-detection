# TODO(M1 · Rohith): every variable the stack reads, with a safe default or a placeholder.
# Copy to .env and fill in. NEVER commit a filled .env — only this example file.

# --- database ---
# DATABASE_URL=postgresql+psycopg2://svaram:svaram@localhost:5432/svaram

# --- queue / cache ---
# REDIS_URL=redis://localhost:6379/0

# --- storage ---
# UPLOAD_DIR=./uploads
# MODEL_DIR=./models

# --- demo identity (no auth system is being built — see README non-goals) ---
# OFFICER_ID=demo-officer-01

# --- toggles ---
# ENABLE_FACE_MATCH=false
# ENABLE_FORENSICS=false
