"""TODO(M2): download PaddleOCR / docTR / InsightFace weights into MODEL_DIR.

Must be idempotent and must not re-download if the files are already present.
Demo day has no reliable network — everything this script fetches has to be cached locally.
"""
